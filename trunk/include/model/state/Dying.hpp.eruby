
<% header_guard do %>

#include "model/state/State.hpp"
namespace psc{ namespace model{ namespace state{

class Dying: public State{
public:
    typedef pDying pointer_type;
    static pState create(wpCube owner){
        return utils::ObjectPool<Dying>::create(owner);
    }

public:
    Dying(wpCube owner): State(owner){}
    virtual ~Dying(){}
    virtual pState cycle();

private:
	int dying_step_;
};

}}} // end of namespace

<% end %>
